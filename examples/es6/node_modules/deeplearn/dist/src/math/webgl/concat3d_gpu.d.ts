import { GPGPUProgram } from './gpgpu_math';
export declare class Concat3DProgram implements GPGPUProgram {
    variableNames: string[];
    params: Array<{}>;
    outputShape: number[];
    userCode: string;
    constructor(x1Shape: [number, number, number], x2Shape: [number, number, number], axis: number);
}
